org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=false
org.gradle.caching=true

# Mod metadata
mod.version=1.2
mod.group=dev.adrian
mod.id=chestindex
mod.name=ChestIndex

# Shared deps
deps.fabric_loader=0.19.5

# Per-version, overridden in versions/<id>/gradle.properties
deps.fabric_api=[VERSIONED]
mod.java=[VERSIONED]
mod.mc_dep=[VERSIONED]
